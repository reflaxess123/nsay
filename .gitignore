# Rust / Cargo
target/
**/Cargo.lock.bak

# Tauri
src-tauri/gen/schemas/

# Node / Vite / SvelteKit
ui/node_modules/
ui/build/
ui/.svelte-kit/
ui/.vite/
ui/dist/

# Local config (committed example, not real secrets)
nsay.toml
nsay.local.toml

# ONNX models — user downloads via Settings → Models
models/*.onnx
models/*.bin
!models/.gitkeep

# CUDA runtime DLLs — fetched via scripts/fetch-cuda-runtime.ps1
# (NVIDIA wheels ~2 GB extracted, never commit)
cuda-deps/
src-tauri/binaries/runtime/*.dll

# ffmpeg bundle — fetched via scripts/fetch-ffmpeg.ps1
# (BtbN GPL build, ffmpeg.exe + ffprobe.exe ~400 MB total, never commit)
src-tauri/binaries/ffmpeg/

# libtorch bundle — fetched via scripts/fetch-libtorch.ps1
# (CUDA 12.4 release ~2.5 GB extracted, never commit)
src-tauri/binaries/libtorch/

# Scratch / workspace junk
icon_new.png
*.tmp.png

# Logs
*.log

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/
