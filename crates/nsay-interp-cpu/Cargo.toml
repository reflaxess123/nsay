[package]
name = "nsay-interp-cpu"
version = "0.1.0"
edition = "2021"
description = "CPU frame interpolation sidecar for nsay (RIFE via ONNX Runtime)"

[workspace]

[[bin]]
name = "nsay-interp-cpu"
path = "src/main.rs"

[dependencies]
ort = { version = "=2.0.0-rc.10", default-features = false, features = ["download-binaries", "ndarray"] }
ndarray = "0.16"
image = { version = "0.25", default-features = false, features = ["png"] }
anyhow = "1"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
